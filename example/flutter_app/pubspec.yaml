name: tbank_invest_flutter_example
description: Flutter example for the tbank_invest package (sandbox GetAccounts).
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.3.0
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter
  tbank_invest:
    path: ../..

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
