# Example

Runs against the **sandbox** with a real token.

```bash
cd ..   # package root
dart run --define=TBANK_TOKEN=t.your_sandbox_token example/example.dart
```

---

**RU:** Запуск с песочницей и реальным токеном — команда выше из корня пакета.
